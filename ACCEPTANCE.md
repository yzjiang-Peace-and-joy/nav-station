# 任务验收标准与指南（ACCEPTANCE GUIDE）

> 本文档定义本项目的验收标准、质量门槛与验收流程。每个功能项按「P0（必须满足）/ P1（应当满足）/ P2（可选加分）」分级。

## 1. 验收总则

1. **可运行性**：`npm install` + `npm run dev` 后浏览器可打开页面，无红屏报错。
2. **一致性**：实现必须同时满足《ARCHITECTURE.md》与《VISUAL.md》的全部约定。
3. **自检清单**：提交代码前逐项核对本文档第 3、4 节的检查表，全部通过方可交付。
4. **分级规则**：
   - **P0** — 核心验收项，缺失/不符 = 验收不通过
   - **P1** — 重要体验项，缺失需说明理由
   - **P2** — 加分项，可延后

---

## 2. 功能验收项

### 2.1 页面渲染与数据（P0）

| # | 验收点 | 判定 |
|---|--------|------|
| F-01 | `npm run dev` 可正常启动，控制台无报错 | 通过/失败 |
| F-02 | 页面按 `sites.json` 渲染出全部分类与站点，分类顺序与 JSON 声明一致 | 通过/失败 |
| F-03 | 无 favicon 时正确降级为名称首字母色块 | 通过/失败 |
| F-04 | 站点卡片点击以新标签页打开目标 URL | 通过/失败 |
| F-05 | JSON 数据缺失/损坏时页面有「数据加载失败」空状态，不白屏 | 通过/失败 |

### 2.2 搜索功能（P0）

| # | 验收点 | 判定 |
|---|--------|------|
| F-06 | 输入关键字即过滤，匹配 `name / desc / tags` 任一命中即显示 | 通过/失败 |
| F-07 | 搜索后空分类区块自动隐藏，只显示有结果的分类 | 通过/失败 |
| F-08 | 清空关键字恢复完整列表 | 通过/失败 |
| F-09 | 无结果时显示友好空状态文案（含「清除搜索」入口） | 通过/失败 |
| F-10 | 输入过程有防抖（≥150ms），避免高频重渲染 | 通过/失败 |
| F-11 | 搜索与标签筛选可叠加生效（AND 关系） | 通过/失败 |

### 2.3 标签筛选（P0）

| # | 验收点 | 判定 |
|---|--------|------|
| F-12 | 标签栏渲染全部标签（来自 `tags[]`，去重） | 通过/失败 |
| F-13 | 点击标签高亮选中并过滤，再点取消 | 通过/失败 |
| F-14 | 标签栏横向溢出时可滚动，滚动条隐藏 | 通过/失败 |

### 2.4 置顶收藏（P0）

| # | 验收点 | 判定 |
|---|--------|------|
| F-15 | 卡片 hover 时星标显现，点击切换置顶状态（按钮，非整卡链接） | 通过/失败 |
| F-16 | 置顶站点进入页面顶部「置顶收藏」区块，并从原分类移除 | 通过/失败 |
| F-17 | 置顶状态刷新页面后保持（localStorage 持久化） | 通过/失败 |
| F-18 | 置顶集合为空时置顶区块整体隐藏 | 通过/失败 |
| F-19 | `sites.json` 中 `pinned: true` 的默认置顶项与用户手动置顶合并去重 | 通过/失败 |

### 2.5 主题切换（P0）

| # | 验收点 | 判定 |
|---|--------|------|
| F-20 | 点击按钮在深色/浅色间切换，全站配色同步变化 | 通过/失败 |
| F-21 | 刷新后主题保持（localStorage 持久化） | 通过/失败 |
| F-22 | 首次访问未手动选择时，跟随系统 `prefers-color-scheme` | 通过/失败 |
| F-23 | 页面加载过程无主题闪烁（FOUC） | 通过/失败 |
| F-24 | 切换主题时页面布局无跳动、无闪烁 | 通过/失败 |

### 2.6 视觉效果（P0）

| # | 验收点 | 判定 |
|---|--------|------|
| V-01 | 完全使用《VISUAL.md》设计令牌，无组件内写死颜色 | 通过/失败 |
| V-02 | 极简风：卡片默认无背景色块、无粗边框，hover 才出现交互态 | 通过/失败 |
| V-03 | 两主题下正文对比度 ≥ 4.5:1 | 通过/失败 |
| V-04 | Header 吸顶且背景半透明模糊 | 通过/失败 |
| V-05 | 全站过渡动画统一 150ms ease | 通过/失败 |
| V-06 | 键盘 Tab 聚焦有可见 focus ring | 通过/失败 |
| V-07 | 响应式：≥1080px 居中、<720px 网格降级为 `minmax(160px,1fr)` | 通过/失败 |

---

## 3. 代码质量检查表（交付前逐项核对）

| # | 检查项 | 说明 |
|---|--------|------|
| C-01 | 目录结构符合《ARCHITECTURE.md》§2 | 无多余文件、无组件缺位 |
| C-02 | 组件不直接 `import sites.json`，不直接读写 localStorage | 职责收敛于 composables |
| C-03 | 组件通信走 props/emit，无跨层引用 | 符合 §5 约定 |
| C-04 | localStorage key 带 `nav_` 前缀 | `nav_theme` / `nav_pinned` |
| C-05 | 全部颜色/间距/圆角走 CSS 变量 | 无魔法数值 |
| C-06 | 无 `!important`、无内联样式写死颜色 | — |
| C-07 | 无控制台 warning / error | 含运行时警告 |
| C-08 | 无未使用变量、无重复代码 | 代码整洁 |
| C-09 | 组件名、事件名符合命名约定 | PascalCase / kebab-case |
| C-10 | 未引入红线依赖 | 见《ARCHITECTURE.md》§8 |

---

## 4. 数据完整性检查表（针对 sites.json）

| # | 检查项 | 通过标准 |
|---|--------|----------|
| D-01 | 分类数量 | 8~10 个 |
| D-02 | 站点总数 | ≥ 50 个 |
| D-03 | 站点 `category` 引用有效 | 100% 命中 `categories[].id` |
| D-04 | 站点 `tags` 属于全局标签池 | 100% 子集 |
| D-05 | `id` 全局唯一 | 无重复 |
| D-06 | `url` 含协议前缀 | 全部为 `https://` 或 `http://` |
| D-07 | 描述 ≤ 40 字 | 无超长描述 |
| D-08 | 分类覆盖均衡 | 每分类站点数 ≥ 4 |

---

## 5. 验收流程（How to Verify）

### 5.1 自动化核对

```bash
npm run dev          # 启动开发服务器
npm run build        # 构建通过，无报错
npm run preview      # 预览产物正常渲染
```

### 5.2 手动核对路径（按顺序执行）

1. **首屏**：打开页面 → 检查主题跟随系统、无 FOUC、无报错
2. **浏览**：滚动检查全部分类、网格布局、favicon 加载与降级
3. **搜索**：输入「Git」「开发」→ 检查过滤结果与空分类隐藏；输入乱码 → 检查空状态
4. **标签**：点击 2~3 个标签 → 检查过滤、再叠加搜索 → 检查 AND 关系
5. **置顶**：hover 卡片星标 → 置顶 → 刷新 → 检查置顶区与持久化；取消置顶 → 检查移除
6. **主题**：切换深/浅色 → 检查配色与布局稳定 → 刷新保持
7. **响应式**：缩放到 720px 以下 → 检查网格与 Header 降级
8. **键盘**：Tab 遍历 → 检查 focus ring 可见

### 5.3 交付物清单

| 交付物 | 位置 |
|--------|------|
| 项目源码 | `nav_station/` 根目录 |
| 架构指导 | `ARCHITECTURE.md` |
| 视觉指导 | `VISUAL.md` |
| 本验收文档 | `ACCEPTANCE.md` |
| 运行说明 | 见 README 或最终交付说明 |

---

## 6. 验收结论判定

| 结论 | 条件 |
|------|------|
| ✅ 验收通过 | 全部 P0 通过，且 P1 无未说明的缺失 |
| ⚠️ 有条件通过 | 存在 ≤3 项 P0 可快速修复项，修复后复验 |
| ❌ 验收不通过 | 存在无法运行、白屏、或 >3 项 P0 未通过 |

> 复验通过后，本次交付即告完成。后续管理后台设计另立新任务，遵守同样的文档先行流程。
